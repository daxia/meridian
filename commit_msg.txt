fix: 解决 TS 类型兼容性错误

- `index.ts`: 显式包装 `app.fetch` 以匹配 `ExportedHandler.fetch` 签名。
- `generateBrief.workflow.ts`: 捕获 `JSON.parse` 异常时显式将 `error` 类型断言为 `unknown`，符合 TS 4.0+ 的 `useUnknownInCatchVariables` 规则。